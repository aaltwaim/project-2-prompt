<h1>Editing Collectible</h1>
 <% if current_user.id == @collectible.user_id %>
 
<%= render 'form', collectible: @collectible %>


<%= link_to 'Show', @collectible %> |
<%= link_to 'Back', collectibles_path %>
<% else%>
<h1>You are not the authorized user to edit</h1>

<%= link_to 'Show', @collectible%>
<% end%>