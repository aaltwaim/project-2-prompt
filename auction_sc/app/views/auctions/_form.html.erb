<%= form_with(model: auction, local: true) do |form| %>
  <% if auction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction.errors.count, "error") %> prohibited this auction from being saved:</h2>

      <ul>
      <% auction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :collectible_id %>
    <%= form.number_field :collectible_id %>
  </div>

  <div class="field">
    <%= form.label :number_of_bidding %>
    <%= form.number_field :number_of_bidding %>
  </div>

  <div class="field">
    <%= form.label :previous_bidding %>
    <%= form.number_field :previous_bidding %>
  </div>

  <div class="field">
    <%= form.label :current_bidding %>
    <%= form.number_field :current_bidding %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
